# 配置笔记

## 已配置连接池的组件
1. PostgreSQL（HikariCP）
2. Redis（Lettuce + commons-pool2）
   [
   {
   "资源名": "PostgreSQL连接池",
   "参数": [
   {"参数名": "数据源连接地址", "参数值": "jdbc:postgresql://39.98.125.91:5432/postgres?connectTimeout=1&socketTimeout=1&tcpKeepAlive=true"},
   {"参数名": "最大连接数", "参数值": 20},
   {"参数名": "最小空闲连接数", "参数值": 5},
   {"参数名": "连接超时时间", "参数值": "500ms"},
   {"参数名": "连接校验超时时间", "参数值": "2000ms"},
   {"参数名": "空闲连接回收时间", "参数值": "300000ms"},
   {"参数名": "连接最大生存时间", "参数值": "300000ms"}
   ],
   "理由": "快速失败并控制连接老化风险"
   },
   {
   "资源名": "Redis连接池",
   "参数": [
   {"参数名": "主机地址", "参数值": "39.98.125.91"},
   {"参数名": "端口", "参数值": 6379},
   {"参数名": "连接超时时间", "参数值": "300ms"},
   {"参数名": "读超时时间", "参数值": "500ms"},
   {"参数名": "最大活跃连接数", "参数值": 20},
   {"参数名": "最大空闲连接数", "参数值": 8},
   {"参数名": "最小空闲连接数", "参数值": 2},
   {"参数名": "最大等待时间", "参数值": "300ms"},
   {"参数名": "空闲连接检测周期", "参数值": "20s"}
   ],
   "理由": "低延迟优先并定期保活连接"
   }
   ]

## Skill 预处理流程总结（附加）

### 1. 输入与入口
1. 前端提交 `curl` 示例（包含 MCP server 配置）。
2. 后端预处理接口接收：`serverLabel`、`skillDescription`、`curlExample`。

### 2. 工具发现与向量化
1. 后端使用小模型调用 MCP `tools/list`，拿到工具列表。
2. 对每个工具构造语义文本（名称+描述+输入 schema）做 embedding。
3. 对每个工具 embedding 做 L2 归一化，得到工具自身向量。

### 3. 技能向量
1. 对技能描述 `skillDescription` 做 embedding。
2. 对技能 embedding 做 L2 归一化，得到技能向量。

### 4. 热度权重与工具包向量
1. 每个工具读取近 7 天调用次数 `w`。
2. 计算历史热度权重：`heat = sigmoid(log10(w + 1))`。
3. 用 `heat` 作为权重对工具向量做加权平均。
4. 对加权平均结果做 L2 归一化，得到工具包向量。

### 5. 最终技能向量融合
1. 融合公式：`final_raw = 0.7 * skill_vec + 0.3 * tool_pkg_vec`。
2. 对 `final_raw` 做 L2 归一化，得到最终技能向量。

### 6. 定时任务（每天凌晨 3 点）
1. 扫描 Redis 的工具调用计数 Hash，生成“今日全局工具调用统计”。
2. 使用 Lua 脚本原子执行：
   - 旧计数 Hash 转存为当日快照；
   - 旧计数通过 GETSET 原子清零；
   - 继续复用同一批计数 key。
3. 将当日统计写入 PostgreSQL 的日统计表。
4. 查询最近 7 天统计表，按工具聚合总调用次数。
5. 将“工具 -> 近 7 日总调用次数”放入 `HashMap<String, Long>` 返回给预处理流程。

### 7. 核心产物
1. 工具语义信息（便于检索）。
2. 工具归一化向量与热度权重。
3. 技能向量、工具包向量、最终技能向量。
4. 每日工具调用统计表与近 7 日汇总映射。

### 8. 热度权重公式（建议保留）
1. 定义：`w` 为工具近 7 日调用次数。
2. 权重公式：`heat(w) = sigmoid(log10(w + 1))`。
3. 展开：`sigmoid(x) = 1 / (1 + e^(-x))`。
4. 作用：压缩大调用量差距，避免头部工具权重过度垄断，同时保留热门趋势。


### 9. 先清洗再向量化的原因
1. 工具原始说明常含噪声字符、重复片段和无关字段，直接 embedding 会把噪声一并编码，降低向量质量。
2. 通过独立模型先做“强格式 JSON 清洗”，可提炼稳定字段（能力、输入、输出、限制），减少语义漂移。
3. 再基于清洗结果生成内聚语义文本做 embedding，可显著提升召回精度、工具区分度和路由一致性。


### 10. 摘要
1. 已新增“工具语义清洗 + 语义压缩”两段式模板机制，先清洗再向量化，减少噪声干扰。
2. 模板文件采用本地 `.md` 维护，支持版本化修改与快速回滚。
3. 配置文件仅保存模板路径，运行时由配置类读取模板内容，业务层直接取用。
4. 关键目标是提升工具语义内聚度、检索稳定性和路由准确率。



### 11. Redis 计数改造经验（去大Key）
1. 原方案将所有工具计数放在同一个 Hash，存在大Key风险（内存、阻塞、迁移都不友好）。
2. 改为“每工具一键”：`tool:{toolId}:count`（string + INCR），天然分散热点。
3. 日切统计不再依赖单点结构，采用 `SCAN + 分批Lua` 扫描计数键并原子处理。
4. `SCAN COUNT` 只是 hint，不保证精确数量；先用 `COUNT=50`，压测后再调大。
5. Lua 不做动态拼接，使用固定模板脚本（文件化管理），避免脚本版本泛滥。
6. 分批脚本每次处理一小批 key：GETSET 取值并清零 -> 写统计，降低单次阻塞。
7. 统计口径采用 PostgreSQL 日表聚合近7日，不在 Redis 中做复杂滑窗减法，维护成本更低。
8. 结论：该方案在可维护性、稳定性和可扩展性上优于“大Hash + 一次性处理”。

### 12.PGSQL统计报表
PostgreSQL 这类按天写、按近 7 天查的统计表，最稳是按 stat_date 做周分区，并在写入前按锚点日期自动预建邻近分区，避免缺分区导致插入失败。
查询侧用日期范围聚合即可，配合分区裁剪通常不是全表扫描。

### 12. 经验
先定单意图，再扩多意图。 单意图意图是工具主标签投票得来的，工具只有一个主标签，原因是市面上大多数mcp server 只提供读服务
写服务一般是自己系统内部的管理操作，因而不会有一个skill既是读，又是写

### 13. 第三方HTTP线程池经验
第三方HTTP调用线程池不能超过下游限流，且不要排队；使用 `SynchronousQueue` 零队列，超载时快速失败或立刻执行。

### 14. WireGuard 安全经验
公网只开 `22/tcp` 与 `51820/udp`，中间件仅绑定 WireGuard 虚拟 IP，避免数据库等服务暴露到公网。

### 15. 运维经验
建表、容器初始化等高风险动作从主链路剥离为独立运维接口，并配前端防抖与请求中禁用，避免重复执行。
运维长命令改为远端脚本执行，前端仅上传与触发，稳定性和可维护性更高。
冷启动后建表前先做就绪探测：MySQL/PG 采用 8 次×4 秒重试，兼顾稳定性与时延。等到容器启动好之后再执行建表命令
RocketMQ 经验：先等 NameServer 就绪（8次×4秒）再启动 Dashboard，失败应快速退出，避免假成功。
docker rm -f 会等“停止+删除”完成后才返回退出码，删除容器和重建容器之间不需要轮询探测
##  16。异常解析器笔记
- @RestControllerAdvice + @ExceptionHandler 底层走 Spring MVC 的 HandlerExceptionResolver 链，核心实现是 ExceptionHandlerExceptionResolver，负责把业务异常映射为统一 HTTP 响应。
防止主线程的异常未被捕获、减少冗余代码

### 17. XxlJobConfig 的 @Bean(initMethod="start", destroyMethod="destroy") 改成了普通 @Bean

- XXL-Job 手工点1：先在调度中心创建执行器分组，AppName必须与 xxl.job.executor.appname 一致。
- XXL-Job 手工点2：执行器分组建议用自动注册，地址列表由执行器心跳自动上报。
- XXL-Job 手工点3：任务不会自动创建，需在任务管理手工新增并绑定 JobHandler。
- XXL-Job 手工点4：首次联调先点“执行一次”，成功后再点“启动”按 Cron 定时跑。
- XXL-Job 手工点5：固定 executor ip/port，避免多实例或重启时端口漂移导致注册不稳定。

### 18.字段简化，明确热度的：
Skill 仅保留单语义向量（skill_vector），移除 tool_package/final，降低写放大与演进成本。
